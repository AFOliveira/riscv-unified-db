# Copyright (c) AI Foundry
# SPDX-License-Identifier: BSD-3-Clause

# yaml-language-server: $schema=../../../../../schemas/inst_schema.json

$schema: inst_schema.json#
kind: instruction
name: cache.flushva
long_name: Cache Flush by Virtual Address
description: |
  Flush dirty cache line by virtual address. Encoded as CSRRW to CSR 0x8BF (U-mode).
  The source register rs1 contains the virtual address.
definedBy:
  anyOf:
    - Xaifoundry
    - Xaifoundrycache
base: 32
encoding:
  match: 100010111111-----001-----1110011
  variables:
    - name: rs1
      location: 19-15
    - name: rd
      location: 11-7
assembly: xd, xs1
access:
  s: always
  u: always
  vs: always
  vu: always
