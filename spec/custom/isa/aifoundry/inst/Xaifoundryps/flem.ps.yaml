# Copyright (c) AI Foundry
# SPDX-License-Identifier: BSD-3-Clause

# yaml-language-server: $schema=../../../../../schemas/inst_schema.json

$schema: inst_schema.json#
kind: instruction
name: flem.ps
long_name: Floating point less than or equal to compare and mask write packed single
description: |
  Writes the value 1 to each mask register md bit if the corresponding single-precision value in vector
  register fs1 is less than or equal to the single-precision value in vector register fs2, otherwise writes the value 0.
  The mask register m0 is used to control execution. Inactive elements shall not generate exceptions and shall not
  update the destination.
  For the purposes of this instruction, the value -0 is considered to be equal to the value +0. Signaling NaN inputs set
  the invalid operation exception flag. If any input is a NaN the result is 0.
  Subnormal inputs are treated as zeros, and set the input denormal exception flag.
definedBy:
  anyOf:
    - extension:
        name: Xaifoundry
    - extension:
        name: Xaifoundryps
encoding:
  match: 1010000----------10000---1111011
  variables:
    - name: rs2
      location: 24-20
    - name: rs1
      location: 19-15
    - name: rd
      location: 9-7
assembly: md, fs1, fs2
access:
  s: always
  u: always
  vs: always
  vu: always
operation(): |
  for (i = 0; i < VLEN/32; i++) {
  if (m0.bit<i>) md.bit<i> = f32_le(fs1.e<i>, fs2.e<i>) ? 1 : 0;
  }
