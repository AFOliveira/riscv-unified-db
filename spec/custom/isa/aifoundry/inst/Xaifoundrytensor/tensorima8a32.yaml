# Copyright (c) AI Foundry
# SPDX-License-Identifier: BSD-3-Clause

# yaml-language-server: $schema=../../../../../../schemas/inst_schema.json

$schema: inst_schema.json#
kind: instruction
name: tensorima8a32
long_name: TensorIMA8A32
description: |
  Multiply two INT8 matrices (A and B), optionally add INT32 accumulator C0, write INT32
  result to FP registers. Uses the IMA units in each VPU lane (4 INT8
  multiply-accumulates per lane per cycle).
  Encoded as CSRRW to tensor_fma CSR (0x801).
definedBy:
  anyOf:
    - Xaifoundry
    - Xaifoundrytensor
base: 32
encoding:
  match: 100000000001-----001-----1110011
  variables:
    - name: rs1
      location: 19-15
    - name: rd
      location: 11-7
assembly: xd, xs1
access:
  s: always
  u: always
  vs: always
  vu: always
